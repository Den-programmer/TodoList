import React from 'react';
import classes from './task.module.css';

const Task = (props) => {
    return (
        <div className={classes.task}>
            
        </div>
    );
}

export default Task;